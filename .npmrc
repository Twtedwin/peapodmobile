# Workspace installs stay at the repo root. Nested copies of React/React Native
# are what produced "Invalid hook call" and missing babel-preset-expo on the
# previous in-place rewrite.
install-strategy=hoisted
